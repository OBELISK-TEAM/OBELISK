export enum BoardAction {
  ADD_BOARD = 'add board',
  SHARE_BOARD = 'share board',
  USER_JOIN_BOARD = 'user join board',
  USER_LEAVE_BOARD = 'user leave board',
  ADD_SLIDE = 'add slide',
  EDIT_SLIDE = 'edit slide',
  REMOVE_SLIDE = 'remove slide',
}
