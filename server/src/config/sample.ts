// it would be nice to have file where default values are stored
// they won't be used in production, but they are useful for development

// instead of using the default values in the code, we can use the
// default values from the config file
